services:
  backend:
    build:
      context: ./backend
    ports:
      - "4000:4000"
  frontend:
    build:
      context: .
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:4000/api
    depends_on:
      - backend
